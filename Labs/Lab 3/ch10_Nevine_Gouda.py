#SOVLE BELOW


def ch10(logfile):
     #load an rdd contianing the log file here
    logs =
    #load an rdd that has only html files
    htmllogs  =
    users    =  #load an rdd that has the ip/user in each rdd element
    return (logs,htmllogs, users)
